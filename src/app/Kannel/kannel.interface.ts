
import { ChangeDetectionStrategy, ChangeDetectorRef} from '@angular/core'
export class Kannel {
    kannelSettingsId: number; // required with minimum 5 chracters
    server : string;

    constructor( ref: ChangeDetectorRef) {
  
  };
    
}